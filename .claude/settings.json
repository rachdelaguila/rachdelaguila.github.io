{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "command -v codex >/dev/null 2>&1 || npm install -g @openai/codex >/dev/null 2>&1; mkdir -p \"$HOME/.codex\"; [ -f \"$HOME/.codex/config.toml\" ] || printf 'model = \"gpt-5.6-sol\"\\n' > \"$HOME/.codex/config.toml\""
          }
        ]
      }
    ]
  }
}
